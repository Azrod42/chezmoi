DATABASE_URL="{{ pass://cealum/DATABASE_URL/local }}"
JWT_ISSUER="{{ pass://cealum/JWT_ISSUER/local }}"
JWT_SECRET="{{ pass://cealum/JWT_SECRET/local }}"
JWT_TTL_SECONDS="{{ pass://cealum/JWT_TTL_SECONDS/local }}"
